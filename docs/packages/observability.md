# wardhook-observability — design decisions

> **This package is in progress.** The design page covering per-node token, cost, and latency tracing, plus the self-contained HTML trace viewer will be
> written alongside the implementation, following the same format as
> [core](core.md) and [guardrails](guardrails.md): the problem, the options
> considered, the decision, and what it costs.

See the [architecture overview](../architecture.md) for how this package
attaches to `wardhook-core` without either importing the other, and the
[project status table](../../README.md#project-status) for current progress.
